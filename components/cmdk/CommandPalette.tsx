"use client";

import * as React from "react";
import { Command } from "@/components/ui/command";

function CommandPalette(): React.JSX.Element {
  return (
    <Command className="rounded-xl shadow-lg">
      {/* wired via config soon */}
    </Command>
  );
}

export default CommandPalette;
