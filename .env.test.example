# Example values only. Real creds live in:
#  - local: .env.test (gitignored)
#  - CI:   GitHub Actions Secrets (TEST_DATABASE_URL)
#
# Do NOT put real URLs here.
TEST_DATABASE_URL=__INJECTED_BY_CI__
NEXT_PUBLIC_E2E_STABLE=true
