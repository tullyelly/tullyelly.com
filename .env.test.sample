# Real Postgres dedicated for tests (no Docker required)
TEST_DATABASE_URL=postgres://user:password@localhost:5432/your_test_db

# Optional: allow Testcontainers *only if* TEST_DATABASE_URL is absent
# Set to "true" to enable fallback, "false" (default) to disable
DOCKER_FALLBACK=false
